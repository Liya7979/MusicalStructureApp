<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppThemeNoBar" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="TabLayout" parent="Widget.Design.TabLayout">
        <item name="tabBackground">@color/colorPrimary</item>
        <item name="tabSelectedTextColor">@color/colorAccent</item>
        <item name="tabTextAppearance">@style/MyTabTextAppearance</item>
        <item name="tabGravity">fill</item>
        <item name="tabMaxWidth">@dimen/zero</item>
        <item name="android:background">@android:color/white</item>
    </style>

    <style name="MyTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textColor">@color/fontColor</item>
    </style>
    <style name="BigTextViewStyle">
        <item name="android:textSize">@dimen/bigText</item>
        <item name="android:textColor">@color/colorAccent</item>
    </style>
    <style name="SmallTextViewStyle">
        <item name="android:textSize">@dimen/itemTextSize</item>
        <item name="android:textColor">@color/black</item>
    </style>
    <style name="ItemStyle">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="SongItemStyle">
        <item name="android:paddingTop">@dimen/itemTopPadding</item>
        <item name="android:paddingLeft">@dimen/itemPadding</item>
        <item name="android:paddingRight">@dimen/itemPadding</item>
        <item name="android:textSize">@dimen/itemTextSize</item>
    </style>
</resources>
